# Lingmo-settings-audio-server

Implements the `com.system76.LingmoSettings.Audio` varlink interface for the `com.system76.LingmoSettings` varlink service. This allows clients to interact with audio node and application volumes over varlink, manage device profiles and routes, and to watch audio device stats in realtime.

